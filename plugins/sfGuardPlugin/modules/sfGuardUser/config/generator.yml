generator:
  class:              sfPropelAdminGenerator
  param:
    model_class:      sfGuardUser
    theme:            default

    list:
      title:   User list
      display: [ =username, created_at, last_login ]
      filters: [ username ]

    edit:
      title: Edit "%%username%%" user
      fields:
        password:
        password_bis:
        permissions:  { type: admin_double_list, params: through_class=sfGuardUserPermission }
        groups:       { type: admin_double_list, params: through_class=sfGuardUserGroup }
        last_login:   { type: plain }
      display:
        "NONE":                   [ username, _password, _password_bis ]
        "Information":            [ last_login ]
        "Permissions and groups": [ is_active, groups, permissions ]
