generator:
  class:              sfPropelAdminGenerator
  param:
    model_class:      Location
    theme:            default
    list:
      max_per_page: 10
      display:        [restaurant, name, address, city, state, zip]
      filters: [restaurant_id]
      actions:
        _create: -
        batch: { name: Batch Update, action: batch }
